import { PriceQuoteErrorInterceptor } from './price-quote-error.interceptor';

describe('PriceQuoteErrorInterceptor', () => {
  it('should be defined', () => {
    expect(new PriceQuoteErrorInterceptor()).toBeDefined();
  });
});
