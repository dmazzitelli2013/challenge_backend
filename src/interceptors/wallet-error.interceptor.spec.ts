import { WalletErrorInterceptor } from './wallet-error.interceptor';

describe('WalletErrorInterceptor', () => {
  it('should be defined', () => {
    expect(new WalletErrorInterceptor()).toBeDefined();
  });
});
